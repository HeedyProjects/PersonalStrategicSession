import {StyleSheet} from 'react-native';

import {LIGHT_GRAY, BASE_GRAY} from '../../../../utils/Colors';

export const styles = StyleSheet.create({
  mainVew: {paddingLeft: 24},
  firstText: {fontSize: 25, fontWeight: '800', color: BASE_GRAY},
  secondText: {color: LIGHT_GRAY, fontSize: 16, marginTop: 10},
});
